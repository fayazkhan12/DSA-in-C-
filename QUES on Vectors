// find the last occurence of the nummber in vecotr

/*
#include<iostream>
#include<vector>

using namespace std; 
int main()
{
    
    
    vector<int>v;
    
    v.push_back(9);
    v.push_back(3);
    v.push_back(22);
    v.push_back(11);
    v.push_back(77);
    v.push_back(55);
    v.push_back(9);
    v.push_back(7);
    v.push_back(88);
    v.push_back(0);
     int idx=-1;
     int x = 55;
     
     
    for(int i=0;i<v.size();i++)
    {
        if(v[i]== x){
        idx=i;
        }
    }
    cout<<"last index of the element is " <<idx;
}

*/
